#include "malloc.h"
#include <stdio.h>

typedef struct stu_s{
	int num;
	struct stu_s *next;
}stu_t;

int main()
{
	stu_t *p, *q, *p1, *head, *qt;
	int n, i;
	scanf("%d\n", &n);
	for(i = 1; i <= n; i++){
		p = malloc(stu);
	}
 
	printf("dellink\n");
}
